spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI}
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: ${DISCOVERY_SERVER_URI}
  sleuth:
    reactor:
      decorate-on-each: false
