spring:
  application:
    name: discovery-checker
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI}
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: ${DISCOVERY_SERVER_URI}
