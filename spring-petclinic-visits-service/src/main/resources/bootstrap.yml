spring:
  application:
    name: visits-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URI}
      fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: ${DISCOVERY_SERVER_URI}
